name: Windows Cloud RDP

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: 1. Enable Remote Desktop & Firewall
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0 ;
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" ;
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 0

      - name: 2. Create Admin Account
        run: |
          $Password = ConvertTo-SecureString "P@ssw0rd1qazXSW@#" -AsPlainText -Force ;
          New-LocalUser -Name "NvDAdmin" -Password $Password -PasswordNeverExpires -ErrorAction SilentlyContinue ;
          Add-LocalGroupMember -Group "Administrators" -Member "NvDAdmin" -ErrorAction SilentlyContinue ;
          net user runneradmin P@ssw0rd1qazXSW@#

      - name: 3. Install & Connect Tailscale VPN
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
        run: |
          Invoke-WebRequest "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile "C:\tailscale.msi" ;
          Start-Process msiexec -ArgumentList "/i C:\tailscale.msi /quiet /norestart" -Wait ;
          Start-Sleep -Seconds 5 ;
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey="$Env:TAILSCALE_AUTHKEY" --hostname="github-rdp-server" ;
          Start-Sleep -Seconds 8 ;
          $ts_ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4 ;
          Add-Content -Path $env:GITHUB_ENV -Value "TS_IP=$ts_ip" ;
          Write-Host "Tailscale IP : $ts_ip"

      - name: 4. Send Info to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          $bot = $env:TELEGRAM_BOT_TOKEN ;
          $chat = $env:TELEGRAM_CHAT_ID ;
          $ip = $env:TS_IP ;
          if ($bot -and $chat) {
            $msg = "New Windows VPS Ready!`n`nTailscale IP: $ip`nUser: NvDAdmin`nPass: P@ssw0rd1qazXSW@#`nValid for: 6 Hours" ;
            Invoke-RestMethod -Uri "https://api.telegram.org/bot$bot/sendMessage" -Method Post -Body @{ chat_id = $chat ; text =$msg } ;
          }

      - name: 5. Keep Server Alive (up to 5.5 Hours)
        run: |
          for ($i = 1; $i -le 330; $i++) {
            Start-Sleep -Seconds 60 ;
            Write-Host "[$i/330] Server running..." ;
          }

      - name: 6. Trigger Next Workflow Automatically
        if: always()
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          $repo = "${{ github.repository }}" ;
          $url = "https://api.github.com/repos/$repo/actions/workflows/windows-rdp.yml/dispatches" ;
          $headers = @{
            "Authorization" = "Bearer $env:GH_PAT"
            "Accept"        = "application/vnd.github.v3+json"
            "User-Agent"    = "PowerShell"
          } ;
          $bodyParams = @{ ref = "main" } | ConvertTo-Json ;
          Invoke-RestMethod -Uri $url -Method Post -Headers $headers -Body$bodyParams ;
